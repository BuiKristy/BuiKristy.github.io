{
  "name": "recipes",
  "version": "1.0.0",
  "description": "recipes compilation website",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch:sass": "sass --watch ./sass:public/stylesheets",
    "nodemon": "nodemon ./index.js",
    "dev": "concurrently --kill-others \"npm run nodemon\" \"npm run watch:sass\""
  },
  "author": "Kristy Bui",
  "license": "ISC",
  "dependencies": {
    "ejs": "^3.1.8",
    "express": "^4.18.2",
    "mongodb": "^5.0.1"
  },
  "devDependencies": {
    "concurrently": "^7.6.0",    
    "nodemon": "^2.0.20"
  }
}
